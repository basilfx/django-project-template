from . import *

# Default settings, overrides generic.py
DEBUG                   = True
TEMPLATE_DEBUG          = True 

# Database settings
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': '',
        'USER': '',
        'PASSWORD': '',
        'HOST': ''
    }
}
